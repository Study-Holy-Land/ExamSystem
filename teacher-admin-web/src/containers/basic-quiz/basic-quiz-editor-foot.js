import {connect} from 'react-redux';
import BasicQuizEditorFoot from '../../components/basic-quiz/basic-quiz-editor-foot';

const mapStateToProps = (state) => state;

export default connect(mapStateToProps)(BasicQuizEditorFoot);
