import BasicQuizList from '../../components/basic-quiz/basic-quiz-list';
import {connect} from 'react-redux';

const mapStateToProps = (state) => state;

export default connect(mapStateToProps)(BasicQuizList);
